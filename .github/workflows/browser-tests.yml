name: Browser tests

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  browser:
    name: Chromium (${{ github.ref_name }})
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22.22.2
          cache: npm
      - name: Install dependencies
        run: npm ci
      - name: Install Playwright Chromium
        run: npx playwright install --with-deps chromium
      - name: Unit and component tests
        run: npm test
      - name: Thirty-day diagnostic
        run: npm run diagnose:30-days
      - name: Prepare browser fixtures
        run: timeout --kill-after=5s 60s npm run test:browser:prepare
      - name: Discover browser tests
        run: timeout --kill-after=5s 60s npm run test:browser:list
      - name: Run browser tests
        env:
          DEBUG: pw:webserver
        run: timeout --kill-after=10s 10m npm run test:browser:run
      - name: Upload browser evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: browser-evidence-${{ github.run_id }}
          if-no-files-found: warn
          retention-days: 14
          path: |
            playwright-report/
            test-results/
            .tmp/browser-fixtures.json
            .wrangler/wrangler.log
            .wrangler/logs/
